*.pyc
.DS_Store
data/
data-step10/
/audit/
backup_data/
vis/*.html
www/index copy.html
/tests/test_data
/over-limit
/files-too-large
*.backup
cities/us_census_city_selection.log
cities/~$SUB-IP-EST2023-POP.xlsx
cities/SUB-IP-EST2023-POP.csv
*.log
/duplicates
# Credentials: .env plus any variant (.env.bak.*, .env.local, ...). Only a
# committed-by-design template is exempt.
.env*
!.env.example
/logs/
*.db
*.db-wal
*.db-shm
__pycache__/
.pytest_cache/
# Experiment artifacts (snapshots, metrics, generated reports/figures). The
# findings that matter are written up in docs/experiments/; these are the raw
# outputs, and they stay out of data/ so the publisher can't rsync them public.
/experiments/
# Driving-plan snapshots (issue #176): dated immutable archive of Google's
# published feed. Outside data/ so the publish rsync never sees it; never
# committed.
/archive/
# Dated catalog backups (issue #145). Outside data/ for the same reason as
# /archive/ — the publish rsync only walks data/, so nothing here can reach the
# public web server. Inside the project root so CSE IT's snapshots cover it.
/backups/
# Per-host lock files (issue #208), serializing access to third parties that
# meter by IP. Runtime state, never committed. Here rather than /tmp because the
# systemd unit sets PrivateTmp=true, which would hide these from exactly the
# detached operator processes the lock exists to serialize against.
/locks/
# In-flight census checkpoints — KartaView's radius sweep (issue #239) and
# Mapillary's tile census (issue #256): a paced crawl commits what it has
# answered and resumes tomorrow. Outside data/ is a hard requirement rather than
# tidiness — the contents are a PARTIAL census, and a partial census that
# reached the publisher would diff against the previous snapshot as every pano
# in the rest of the city being removed.
/checkpoints/
# Claude Code. settings.json is shared project config and IS tracked (team
# settings: permissions, hooks); the local override and worktree metadata are
# per-machine. Claude Code normally protects settings.local.json by adding it
# to the GLOBAL git excludes file (~/.config/git/ignore) on the machine where
# it first writes one — which does not travel with the repo, so a checkout on
# another host (makelab2 has no global excludes at all) would see it as an
# ordinary untracked file and sweep it into a `git add -A`.
.claude/settings.local.json
.claude/worktrees/
# Virtualenvs and the uv toolchain. Python's `venv` writes a self-ignoring
# `.venv/.gitignore` containing `*`, which is why this repo got away without an
# entry — but that protection lives INSIDE the venv, so it is absent from any
# env created another way. On makelab2 the checkout's `.venv/` has no such file,
# and `git add -A` there would have staged 7,362 files into this public repo.
# Cover every variant, since the production interpreter is `.venv-makelab2/`.
.venv/
.venv-*/
.tooling/

# Completed censuses promoted out of checkpoints/ and shared across the channels
# that read the same (provider, city, bbox) observation (issue #290). Same two
# reasons as checkpoints/ above -- provider bulk data, and never publishable --
# plus one of its own: this one is deliberately reachable by every consumer, so
# a committed entry would be a census the next collection silently republishes.
# The scheduler's tail prunes it; nothing here is ever needed to rebuild an
# artifact.
/census_cache/
